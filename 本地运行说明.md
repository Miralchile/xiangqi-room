# 本地运行

在 Finder 中打开克隆后的项目文件夹。

双击 `启动象棋.command` 会执行以下操作：

1. 启动 `http://127.0.0.1:3000` 的本地象棋服务。
2. 重启 cpolar 中国区隧道，以适应 Wi-Fi、热点或代理切换。
3. 等待随机公网域名生成，将地址复制到剪贴板并弹窗显示。

公网地址是 `https://随机字符.r9.cpolar.cn` 一类的域名。访问网站的手机或电脑无需登录 cpolar，也无需运行 ChatGPT。主机需要保持开机、联网，且不能进入深度睡眠。

双击 `关闭象棋.command` 会关闭本地服务和 cpolar 隧道。关闭后原公网地址立即失效；再次启动通常会获得新的随机地址。

若 macOS 首次双击时阻止执行，可在 Finder 中右键脚本，选择“打开”，再确认一次。
